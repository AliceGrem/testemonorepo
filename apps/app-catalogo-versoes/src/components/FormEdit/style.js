
import { spacings, utils } from '@monorepo/theme';
import styled from 'styled-components';

export const StyledFormEdit = styled.div`
  height: ${utils.height.full};
  width: ${utils.width.full};
  margin-left: ${spacings.margin.spacing4xl};
  `

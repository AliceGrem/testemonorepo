import { fonts } from './fonts';
import { spacings } from './spacings';

export const types = {
  labelEnabled: {
    fontWeight: fonts.weight.fontSemiBold,
    lineHeight: spacings.lineHeight.lineMd,
  },

  labelActive: {
    fontWeight: fonts.weight.fontLight,
    lineHeight: spacings.lineHeight.lineXs,
  },
};

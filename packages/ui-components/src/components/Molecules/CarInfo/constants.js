import PropTypes from 'prop-types';

export const CAR_INFO_LABEL = 'Placa:';

export const propTypes = {
  licencePlate: PropTypes.string,
  logo: PropTypes.string,
  description: PropTypes.string,
};
